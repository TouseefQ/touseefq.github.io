/**
 * Form Handler
 * ============
 * Stub for future Formspree integration (Phase 3).
 * Currently, Formspree handles form submission via standard form action.
 *
 * Future enhancements:
 * - Success/error message handling
 * - Form validation
 * - Loading states
 * - reCAPTCHA integration
 */

document.addEventListener('DOMContentLoaded', () => {
  // Form handling will be added during Phase 3
  // For now, Formspree handles submission via form action attribute
});
